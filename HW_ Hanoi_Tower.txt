function tower(n, src, dest, temp) {
  if (n == 0) {
    return
  } else {
    tower(n - 1, src, temp, dest);
    console.log(src + " -> " + dest);
    tower(n - 1, temp, dest, src);
  }
}
tower(5, 1, 2, 3);

Много времени потратил на попытку решить задачу итеративным методом, но пока решения у меня нет.
Надеюсь, позже вернусь к этому. На данный момент не могу выделить столько времени на поиск решения, 
получается в ущерб остальным предметам.